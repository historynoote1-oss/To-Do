# Railway بيولّد المتغير ده تلقائيًا لو ضفت PostgreSQL كإضافة في نفس المشروع
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# نص عشوائي طويل يُستخدم لتوقيع جلسات الدخول (JWT). غيّره لأي نص عشوائي طويل بتاعك
JWT_SECRET=change_this_to_a_long_random_string

# رابط الفرونت إند بتاعك من Vercel (عشان CORS)، مثال: https://todo.vercel.app
FRONTEND_URL=

PORT=3001

# ===== تخزين صور الأفتار وأيقونات المجالات (Cloudinary) =====
# اختياري لكن مهم على استضافات زي Railway: نظام الملفات المحلي بيتصفّر مع كل
# نشر جديد، فمن غير المتغيرات دي الصور المرفوعة هتضيع أول ما تعمل deploy تاني.
# حساب مجاني كافي: https://cloudinary.com — لو سبتها فاضية، النظام هيرجع
# تلقائيًا للتخزين المحلي (شوف lib/uploads/avatarUpload.ts).
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ===== إشعارات الجهاز (Web Push) لنظام التذكيرات =====
# ولّد الزوج ده مرة واحدة بالأمر: npx web-push generate-vapid-keys
# نفس الـ VAPID_PUBLIC_KEY لازم يتحط برضو في frontend/.env كـ VITE_VAPID_PUBLIC_KEY
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# ===== صفحة "مشغّل الصوت" (بحث وتشغيل يوتيوب) =====
# مفتاح YouTube Data API v3 — لازم يتحط هنا بس (متغير بيئة على السيرفر)،
# ومينفعش يتحط في أي كود أو ملف بيوصل للفرونت إند أبدًا. الفرونت إند
# بيكلّم مسار /api/youtube/search بتاع السيرفر، والسيرفر هو اللي بيضيف
# المفتاح ده ويكلّم يوتيوب من ورا الكواليس — فمفيش أي مستخدم يقدر يشوف
# المفتاح حتى لو فتح "عرض المصدر" أو أدوات المطوّر في المتصفح.
# هاته من: https://console.cloud.google.com/apis/library/youtube.googleapis.com
YOUTUBE_API_KEY=
