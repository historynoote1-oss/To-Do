# ⚠️ الملف ده فيه أسرار حقيقية — متبعتوش لحد، ومترفعوش على GitHub أو أي
# مكان عام (.gitignore جوه المجلد ده مضبوط بالفعل عشان يمنع رفعه بالغلط).
#
# لو بترفع الباك إند على Railway/Render/أي استضافة، متعتمدش على الملف ده
# وانت شغّال فعليًا — انسخ نفس المتغيرات دي في قسم "Environment Variables"
# بتاع لوحة تحكم الاستضافة، لأن الملف ده بيفضل على جهازك بس ومش بيترفع مع
# الكود (لو استخدمت git، وده المفروض).
#
# باقي المتغيرات (DATABASE_URL، JWT_SECRET، FRONTEND_URL، VAPID...) لسه
# لازم تحطها إنت بنفسك زي ما موضّح في .env.example — الملف ده بيضيف بس
# متغير مفتاح يوتيوب الجاهز عشان صفحة "مشغّل الصوت" تشتغل على طول.

YOUTUBE_API_KEY=AIzaSyAnJobki-jLqTsbMct3cC7BMXD-Cwps7SU
